<% flash.each do |type, message| %>
  <div class="mt20 alert <%= bootstrap_class_for(type) %> fade in">
    <button class="close" data-dismiss="alert">x</button>
    <%= message %>
  </div>
<% end %>
